{
  "name": "resumate-apk",
  "version": "1.1.0",
  "description": "ResuMate resume studio Android app",
  "private": true,
  "scripts": {
    "build": "npm run build:web",
    "build:web": "python3 scripts/build-web.py",
    "sync": "npx cap sync android",
    "android:debug": "npx cap sync android && cd android && ./gradlew assembleDebug --no-daemon",
    "test": "npm run build:web",
    "test:web": "npm run build:web"
  },
  "keywords": ["resumate", "resume", "capacitor", "android"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "docx": "^8.5.0"
  }
}
